<Window x:Class="MilkTeaShop.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MilkTeaShop POS" Height="520" Width="860">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left: Menu (placeholder) -->
        <StackPanel Grid.Column="0" Margin="0,0,8,0">
            <TextBlock Text="Menu (demo)" FontSize="18" FontWeight="Bold" Margin="0 0 0 8"/>
            <Button Content="Add Milk Tea + Pearl" Margin="0 0 0 6" Command="{Binding AddPearlTeaCommand}"/>
            <Button Content="Add Milk Tea + Cheese Foam" Margin="0 0 0 6" Command="{Binding AddCheeseTeaCommand}"/>
            <Button Content="New Order" Command="{Binding NewOrderCommand}" Click="Button_Click" />
        </StackPanel>

        <!-- Right: Cart -->
        <StackPanel Grid.Column="1">
            <TextBlock Text="Current Order" FontSize="18" FontWeight="Bold" Margin="0 0 0 8"/>
            <ListBox ItemsSource="{Binding Items}" Height="300"/>
            <TextBlock Text="{Binding SubtotalText}" Margin="0 8 0 0" />
            <TextBlock Text="{Binding DiscountText}" />
            <TextBlock Text="{Binding TotalText}" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                <Button Content="Apply 20% OFF" Command="{Binding ApplyPercentOffCommand}" Margin="0 0 8 0"/>
                <Button Content="Checkout" Command="{Binding CheckoutCommand}" />
            </StackPanel>
            <TextBlock Text="{Binding StatusText}" Margin="0 8 0 0" />
        </StackPanel>
    </Grid>
</Window>
