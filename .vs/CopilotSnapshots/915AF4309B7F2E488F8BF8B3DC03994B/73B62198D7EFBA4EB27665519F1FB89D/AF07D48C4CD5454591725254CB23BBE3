using System.Windows;
using MilkTeaShop.Presentation.ViewModels;

namespace MilkTeaShop.Presentation;

public partial class SettingsWindow : Window
{
    public SettingsWindow()
    {
        InitializeComponent();
        DataContext = new SettingsViewModel();
    }

    private void Close_Click(object sender, RoutedEventArgs e)
    {
        DialogResult = false;
        Close();
    }

    private void ViewDatabase_Click(object sender, RoutedEventArgs e)
    {
        try
        {
            var databaseViewer = new DatabaseViewerWindow();
            databaseViewer.Owner = this;
            databaseViewer.ShowDialog();
        }
        catch (System.Exception ex)
        {
            MessageBox.Show($"Lỗi mở Database Viewer: {ex.Message}", "Lỗi", 
                           MessageBoxButton.OK, MessageBoxImage.Error);
        }
    }
}