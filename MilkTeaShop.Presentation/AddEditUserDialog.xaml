<Window x:Class="MilkTeaShop.Presentation.AddEditUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm/Sửa người dùng" Height="400" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText" Text="THÊM NGƯỜI DÙNG" FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Tên đăng nhập:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="UsernameTextBox" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="3" Text="Mật khẩu:" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="4" x:Name="PasswordBox" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="5" Text="Họ tên:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="6" x:Name="FullNameTextBox" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="7" Text="Vai trò:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="8" x:Name="RoleComboBox" Margin="0,0,0,15">
            <ComboBoxItem Content="Admin" Tag="Admin"/>
            <ComboBoxItem Content="Manager" Tag="Manager"/>
            <ComboBoxItem Content="Employee" Tag="Employee"/>
        </ComboBox>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Lưu" Width="80" Height="35" Margin="0,0,10,0"
                    Click="Save_Click" Background="#32CD32" Foreground="White" FontWeight="Bold"/>
            <Button Content="Hủy" Width="80" Height="35" 
                    Click="Cancel_Click" Background="Gray" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>